  <!-- Video Player Section  -->
  <!-- <div class="hidden">
    <div class="sm:hidden mt-20 mb-6">
        <img class="mx-auto px-[10%]" src="/academy/video-mobile-1.svg" alt="">
    </div>
    <div class="hidden sm:block mt-20 mb-10 max-w-[1440px] mx-auto px-[10%]"><img src="/academy/video-section-3.svg"
            alt=""></div>
    <div class="flex flex-col justify-start px-[10%] mx-auto"  >
        <div class="w-full max-w-[1440px] mx-auto"><img class="w-full" src="/academy/video-section-2.svg"
                alt=""></div>
        <div class="relative w-full max-w-[1440px] mx-auto"> -->
            <!-- Video Element -->
            <!-- <video id="videoPlayer" class="w-full h-auto object-cover rounded-b-sm sm:rounded-b-md md:rounded-b-lg"
                poster="academy/video-section-1.jpg">
                <source src="" type="video/mp4">
                Your browser does not support the video tag.
            </video> -->
  
            <!-- Play Button Overlay -->
            <!-- <button id="playButton" class="absolute inset-0 flex items-center justify-center rounded-lg">
                <div class="flex items-center justify-center w-8 h-8 sm:w-16 sm:h-16 bg-white bg-opacity-75 rounded-full">
                    <svg class="w-4 h-4 sm:w-8 sm:h-8 text-gray-800" fill="currentColor" viewBox="0 0 24 24">
                        <path d="M8 5v14l11-7L8 5z" />
                    </svg>
                </div>
            </button>
        </div>
    </div>
  </div> -->
  
  <!-- Video Player Section JavaScript  -->
<!-- <script>
    document.addEventListener('DOMContentLoaded', () => {
        const videoPlayer = document.getElementById('videoPlayer');
        const playButton = document.getElementById('playButton');
  
        playButton.addEventListener('click', () => {
            if (videoPlayer.paused) {
                videoPlayer.play();
                playButton.classList.add('hidden');
            } else {
                videoPlayer.pause();
                playButton.classList.remove('hidden');
            }
        });
  
        videoPlayer.addEventListener('ended', () => {
            playButton.classList.remove('hidden');
        });
    });
  </script> -->